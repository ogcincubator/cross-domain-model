# Strict SHACL shapes for the W3C PROV-O ontology (http://www.w3.org/ns/prov#),
# mechanically derived from PROV-O's OWL axioms (rdfs:range per object property).
#
# Generated by generate-shapes.py in this directory -- see that script's module
# docstring for the full rationale. Do not hand-edit below this header; re-run the
# script instead.
#
# This is the strict counterpart to ../prov/shapes.shacl (this block's dependsOn):
# where that file only requires sh:class when a value is given as a blank node, this
# file requires it for every value actually *described* in the graph (has at least one
# outgoing triple) -- blank node or by-reference IRI alike. A value with no outgoing
# triples at all is a pure external reference this graph could not type even in
# principle, so it is exempt (see <#NotDescribedInGraph> below) rather than rejected.
# Running the base block's `entail` transform (../prov/transforms.yaml) first still
# matters: it types every PROV object-property value from the property assertion
# itself, regardless of local description, which is the only way an undescribed value
# can satisfy the sh:class alternative instead of merely falling back to the
# <#NotDescribedInGraph> exemption.
#
# Each shape below targets every subject of the property in question
# (sh:targetSubjectsOf) instead of restating PROV-O's class hierarchy and
# domain-to-property associations, which are already expressed by ../prov/shapes.shacl
# and inherited automatically at validation time via dependsOn -- so nothing here
# duplicates that file. Datatype (literal-valued) properties are unaffected by the
# typing relaxation this block addresses and are therefore left entirely to the base
# block's shapes.

@prefix sh:   <http://www.w3.org/ns/shacl#> .
@prefix rdfs: <http://www.w3.org/2000/01/rdf-schema#> .
@prefix prov: <http://www.w3.org/ns/prov#> .
@base <https://www.ogc.org/rules/prov/strict/> .

# Shared by every property shape below via sh:or: a node conforms to this shape
# exactly when it has no outgoing triples in the data graph, i.e. it is not itself
# described here -- only referenced. sh:sparql's SELECT reports a violation (so this
# shape does *not* hold) for any $this that has at least one outgoing triple.
<#NotDescribedInGraph>
    a sh:NodeShape ;
    sh:sparql [
        a sh:SPARQLConstraint ;
        sh:message "Value is described in this graph and must therefore be explicitly typed." ;
        sh:select """
            SELECT $this
            WHERE { $this ?p ?o . }
        """ ;
    ] .

<#prop-actedOnBehalfOf>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:actedOnBehalfOf ;
    sh:path prov:actedOnBehalfOf ;
    rdfs:isDefinedBy prov:actedOnBehalfOf ;
    sh:or ( [ sh:class prov:Agent ] <#NotDescribedInGraph> ) .

<#prop-activity>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:activity ;
    sh:path prov:activity ;
    rdfs:isDefinedBy prov:activity ;
    sh:or ( [ sh:class prov:Activity ] <#NotDescribedInGraph> ) .

<#prop-agent>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:agent ;
    sh:path prov:agent ;
    rdfs:isDefinedBy prov:agent ;
    sh:or ( [ sh:class prov:Agent ] <#NotDescribedInGraph> ) .

<#prop-alternateOf>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:alternateOf ;
    sh:path prov:alternateOf ;
    rdfs:isDefinedBy prov:alternateOf ;
    sh:or ( [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .

<#prop-atLocation>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:atLocation ;
    sh:path prov:atLocation ;
    rdfs:isDefinedBy prov:atLocation ;
    sh:or ( [ sh:class prov:Location ] <#NotDescribedInGraph> ) .

<#prop-entity>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:entity ;
    sh:path prov:entity ;
    rdfs:isDefinedBy prov:entity ;
    sh:or ( [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .

<#prop-generated>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:generated ;
    sh:path prov:generated ;
    rdfs:isDefinedBy prov:generated ;
    sh:or ( [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .

<#prop-hadActivity>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:hadActivity ;
    sh:path prov:hadActivity ;
    rdfs:isDefinedBy prov:hadActivity ;
    sh:or ( [ sh:class prov:Activity ] <#NotDescribedInGraph> ) .

<#prop-hadGeneration>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:hadGeneration ;
    sh:path prov:hadGeneration ;
    rdfs:isDefinedBy prov:hadGeneration ;
    sh:or ( [ sh:class prov:Generation ] <#NotDescribedInGraph> ) .

<#prop-hadMember>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:hadMember ;
    sh:path prov:hadMember ;
    rdfs:isDefinedBy prov:hadMember ;
    sh:or ( [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .

<#prop-hadPlan>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:hadPlan ;
    sh:path prov:hadPlan ;
    rdfs:isDefinedBy prov:hadPlan ;
    sh:or ( [ sh:class prov:Plan ] <#NotDescribedInGraph> ) .

<#prop-hadPrimarySource>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:hadPrimarySource ;
    sh:path prov:hadPrimarySource ;
    rdfs:isDefinedBy prov:hadPrimarySource ;
    sh:or ( [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .

<#prop-hadRole>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:hadRole ;
    sh:path prov:hadRole ;
    rdfs:isDefinedBy prov:hadRole ;
    sh:or ( [ sh:class prov:Role ] <#NotDescribedInGraph> ) .

<#prop-hadUsage>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:hadUsage ;
    sh:path prov:hadUsage ;
    rdfs:isDefinedBy prov:hadUsage ;
    sh:or ( [ sh:class prov:Usage ] <#NotDescribedInGraph> ) .

<#prop-influencer>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:influencer ;
    sh:path prov:influencer ;
    rdfs:isDefinedBy prov:influencer ;
    sh:or ( [ sh:class prov:Thing ] <#NotDescribedInGraph> ) .

<#prop-invalidated>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:invalidated ;
    sh:path prov:invalidated ;
    rdfs:isDefinedBy prov:invalidated ;
    sh:or ( [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .

<#prop-qualifiedAssociation>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedAssociation ;
    sh:path prov:qualifiedAssociation ;
    rdfs:isDefinedBy prov:qualifiedAssociation ;
    sh:or ( [ sh:class prov:Association ] <#NotDescribedInGraph> ) .

<#prop-qualifiedAttribution>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedAttribution ;
    sh:path prov:qualifiedAttribution ;
    rdfs:isDefinedBy prov:qualifiedAttribution ;
    sh:or ( [ sh:class prov:Attribution ] <#NotDescribedInGraph> ) .

<#prop-qualifiedCommunication>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedCommunication ;
    sh:path prov:qualifiedCommunication ;
    rdfs:isDefinedBy prov:qualifiedCommunication ;
    sh:or ( [ sh:class prov:Communication ] <#NotDescribedInGraph> ) .

<#prop-qualifiedDelegation>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedDelegation ;
    sh:path prov:qualifiedDelegation ;
    rdfs:isDefinedBy prov:qualifiedDelegation ;
    sh:or ( [ sh:class prov:Delegation ] <#NotDescribedInGraph> ) .

<#prop-qualifiedDerivation>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedDerivation ;
    sh:path prov:qualifiedDerivation ;
    rdfs:isDefinedBy prov:qualifiedDerivation ;
    sh:or ( [ sh:class prov:Derivation ] <#NotDescribedInGraph> ) .

<#prop-qualifiedEnd>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedEnd ;
    sh:path prov:qualifiedEnd ;
    rdfs:isDefinedBy prov:qualifiedEnd ;
    sh:or ( [ sh:class prov:End ] <#NotDescribedInGraph> ) .

<#prop-qualifiedGeneration>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedGeneration ;
    sh:path prov:qualifiedGeneration ;
    rdfs:isDefinedBy prov:qualifiedGeneration ;
    sh:or ( [ sh:class prov:Generation ] <#NotDescribedInGraph> ) .

<#prop-qualifiedInfluence>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedInfluence ;
    sh:path prov:qualifiedInfluence ;
    rdfs:isDefinedBy prov:qualifiedInfluence ;
    sh:or ( [ sh:class prov:Influence ] <#NotDescribedInGraph> ) .

<#prop-qualifiedInvalidation>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedInvalidation ;
    sh:path prov:qualifiedInvalidation ;
    rdfs:isDefinedBy prov:qualifiedInvalidation ;
    sh:or ( [ sh:class prov:Invalidation ] <#NotDescribedInGraph> ) .

<#prop-qualifiedPrimarySource>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedPrimarySource ;
    sh:path prov:qualifiedPrimarySource ;
    rdfs:isDefinedBy prov:qualifiedPrimarySource ;
    sh:or ( [ sh:class prov:PrimarySource ] <#NotDescribedInGraph> ) .

<#prop-qualifiedQuotation>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedQuotation ;
    sh:path prov:qualifiedQuotation ;
    rdfs:isDefinedBy prov:qualifiedQuotation ;
    sh:or ( [ sh:class prov:Quotation ] <#NotDescribedInGraph> ) .

<#prop-qualifiedRevision>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedRevision ;
    sh:path prov:qualifiedRevision ;
    rdfs:isDefinedBy prov:qualifiedRevision ;
    sh:or ( [ sh:class prov:Revision ] <#NotDescribedInGraph> ) .

<#prop-qualifiedStart>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedStart ;
    sh:path prov:qualifiedStart ;
    rdfs:isDefinedBy prov:qualifiedStart ;
    sh:or ( [ sh:class prov:Start ] <#NotDescribedInGraph> ) .

<#prop-qualifiedUsage>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:qualifiedUsage ;
    sh:path prov:qualifiedUsage ;
    rdfs:isDefinedBy prov:qualifiedUsage ;
    sh:or ( [ sh:class prov:Usage ] <#NotDescribedInGraph> ) .

<#prop-specializationOf>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:specializationOf ;
    sh:path prov:specializationOf ;
    rdfs:isDefinedBy prov:specializationOf ;
    sh:or ( [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .

<#prop-used>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:used ;
    sh:path prov:used ;
    rdfs:isDefinedBy prov:used ;
    sh:or ( [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .

<#prop-wasAssociatedWith>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:wasAssociatedWith ;
    sh:path prov:wasAssociatedWith ;
    rdfs:isDefinedBy prov:wasAssociatedWith ;
    sh:or ( [ sh:class prov:Agent ] <#NotDescribedInGraph> ) .

<#prop-wasAttributedTo>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:wasAttributedTo ;
    sh:path prov:wasAttributedTo ;
    rdfs:isDefinedBy prov:wasAttributedTo ;
    sh:or ( [ sh:class prov:Agent ] <#NotDescribedInGraph> ) .

<#prop-wasDerivedFrom>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:wasDerivedFrom ;
    sh:path prov:wasDerivedFrom ;
    rdfs:isDefinedBy prov:wasDerivedFrom ;
    sh:or ( [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .

<#prop-wasEndedBy>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:wasEndedBy ;
    sh:path prov:wasEndedBy ;
    rdfs:isDefinedBy prov:wasEndedBy ;
    sh:or ( [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .

<#prop-wasGeneratedBy>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:wasGeneratedBy ;
    sh:path prov:wasGeneratedBy ;
    rdfs:isDefinedBy prov:wasGeneratedBy ;
    sh:or ( [ sh:class prov:Activity ] <#NotDescribedInGraph> ) .

<#prop-wasInfluencedBy>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:wasInfluencedBy ;
    sh:path prov:wasInfluencedBy ;
    rdfs:isDefinedBy prov:wasInfluencedBy ;
    sh:or ( [ sh:class prov:Activity ] [ sh:class prov:Agent ] [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .

<#prop-wasInformedBy>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:wasInformedBy ;
    sh:path prov:wasInformedBy ;
    rdfs:isDefinedBy prov:wasInformedBy ;
    sh:or ( [ sh:class prov:Activity ] <#NotDescribedInGraph> ) .

<#prop-wasInvalidatedBy>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:wasInvalidatedBy ;
    sh:path prov:wasInvalidatedBy ;
    rdfs:isDefinedBy prov:wasInvalidatedBy ;
    sh:or ( [ sh:class prov:Activity ] <#NotDescribedInGraph> ) .

<#prop-wasQuotedFrom>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:wasQuotedFrom ;
    sh:path prov:wasQuotedFrom ;
    rdfs:isDefinedBy prov:wasQuotedFrom ;
    sh:or ( [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .

<#prop-wasRevisionOf>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:wasRevisionOf ;
    sh:path prov:wasRevisionOf ;
    rdfs:isDefinedBy prov:wasRevisionOf ;
    sh:or ( [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .

<#prop-wasStartedBy>
    a sh:PropertyShape ;
    sh:targetSubjectsOf prov:wasStartedBy ;
    sh:path prov:wasStartedBy ;
    rdfs:isDefinedBy prov:wasStartedBy ;
    sh:or ( [ sh:class prov:Entity ] <#NotDescribedInGraph> ) .
